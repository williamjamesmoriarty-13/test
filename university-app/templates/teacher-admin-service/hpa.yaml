apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.teacherAdminService.name }}
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.teacherAdminService.name }}
  minReplicas: {{ .Values.teacherAdminService.replicas.min }}
  maxReplicas: {{ .Values.teacherAdminService.replicas.max }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.teacherAdminService.targetCpuUtilization }}
