apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.enrollmentService.name }}
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.enrollmentService.name }}
  minReplicas: {{ .Values.enrollmentService.replicas.min }}
  maxReplicas: {{ .Values.enrollmentService.replicas.max }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.enrollmentService.targetCpuUtilization }}
